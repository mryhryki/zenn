{
  "name": "portfolio",
  "author": "mryhryki",
  "private": true,
  "scripts": {
    "article:add": "zenn new:article --slug \"$(date +%Y-%m-%d)-$(openssl rand -hex 3)\"",
    "article:preview": "mkdir -p ./books && zenn preview",
    "portfolio:update": "",
    "portfolio:preview": "serve ./mryhryki.com/"
  },
  "dependencies": {
    "serve": "^12.0.0",
    "zenn-cli": "^0.1.93"
  }
}
