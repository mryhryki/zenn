{
  "name": "portfolio",
  "author": "mryhryki",
  "private": true,
  "scripts": {
    "article:add": "zenn new:article --slug \"$(date +%Y-%m-%d)-$(openssl rand -hex 3)\"",
    "article:preview": "mkdir -p ./books && zenn preview",
    "portfolio:update": "aws s3 sync docs/ s3://mryhryki-cdn/contents/",
    "portfolio:preview": "serve docs/"
  },
  "dependencies": {
    "serve": "^12.0.0",
    "zenn-cli": "^0.1.93"
  }
}
