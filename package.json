{
  "name": "portfolio",
  "author": "mryhryki",
  "private": true,
  "scripts": {
    "article:add": "zenn new:article --slug \"$(date +%Y-%m-%d)-$(openssl rand -hex 3)\"",
    "article:preview": "open http://localhost:58125/ && zenn preview --port 58125",
    "site:update": "aws s3 sync docs/ s3://mryhryki-cdn/contents/ --exclude='.DS_Store' --size-only",
    "site:preview": "open http://localhost:58125/ && serve -p 58125 docs/"
  },
  "dependencies": {
    "serve": "^12.0.1",
    "zenn-cli": "^0.1.98"
  }
}
